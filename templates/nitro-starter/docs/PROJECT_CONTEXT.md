# Contexto del proyecto

El comando `landing:new` reemplaza este contenido con cliente, fuente, modo,
ruta local y ubicación del registro central. Un agente debe leerlo al iniciar y
actualizar el registro al cambiar de etapa.
